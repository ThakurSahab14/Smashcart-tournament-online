# Leave blank for local dev — the client auto-detects the API on the same
# hostname, port 4000. Only set this if your server is exposed at a
# different address (e.g. a second ngrok tunnel dedicated to the API).
# VITE_API_URL=https://your-api-tunnel.ngrok-free.app
